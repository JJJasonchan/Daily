import SwiftUI

enum ColorTokens {
    static let accent = Color.accentColor
    static let accentLight = Color.accentColor.opacity(0.10)
    static let checkboxIncomplete = Color.accentColor
    static let checkboxComplete = Color.accentColor
    static let sectionHeader = Color.secondary
}
