# Daily 手动验收清单

状态只使用 `PASS`、`FAIL`、`BLOCKED`。`BLOCKED` 表示当前验证环境无法安全或可靠地完成该项，不代表功能通过。

## 验证环境

- macOS 版本与 build：26.6.1（25G76）
- Xcode / Swift：Xcode 26.6（17F113），Swift 6.3.3
- 应用包：`build/Daily.app`
- 验证日期：2026-08-12

## 功能与数据

- [x] `PASS` 创建单次任务，今日列表即时显示；新增两项后计数由 0/0 更新为 0/2。
- [x] `PASS` 编辑未完成任务标题；完成任务菜单不提供编辑，取消完成后双击可重新进入编辑器。
- [ ] `BLOCKED` 未执行删除，避免清除验证数据；删除分支有 111 项自动测试覆盖，但本项不以自动测试替代手动通过。
- [x] `PASS` 将首项拖至第二项下方，AX 列表顺序与重启后的顺序均为 B、A；连续让位和中途反向中断无法由静态捕获可靠判定，见动效项。
- [x] `PASS` 快速添加可用，`Command-Shift-A` 返回今日并将焦点置于“新任务标题”。
- [x] `PASS` `Command-1` 至 `Command-4` 分别导航到今日、重复规则、历史记录、设置。
- [ ] `BLOCKED` Rules 页面已手动验证创建、停用和启用；编辑、重排、删除未执行，历史保留仅有自动测试证据。
- [x] `PASS` History 页面展示 7 天周视图，8 月 12 日为 0/2，任务状态为未完成；跨日状态未手动制造。
- [x] `PASS` Settings 页面将持续提醒间隔从 15 改为 10 分钟，保存并重启后仍为 10 分钟；每日提醒未启用，避免触发权限请求。
- [x] `PASS` 终止并重启应用后，三项任务、编辑后的标题、拖排顺序、重复规则和 10 分钟设置保持。
- [ ] `BLOCKED` 主窗口与菜单栏共用模型已有自动测试覆盖；当前 Computer Use AX 无法单独定位 `MenuBarExtra` 状态项，未把不可观察行为记为通过。
- [ ] `BLOCKED` 单次未完成任务跨日后只顺延一份，顺延天数增加；连续多日不产生重复的当前实例。
- [x] `PASS` 手动创建“每天”规则后今日只生成一个“每日复盘”实例，Rules 页面显示每天且已启用；工作日和指定星期仅有自动测试证据。
- [ ] `BLOCKED` 系统日期、时间或时区变化后重新计算本地日期并重建未来通知，数据无重复。

## 通知

- [ ] `BLOCKED` 通知权限拒绝时任务管理仍可用，设置页显示不可用并提供系统设置入口，应用不反复请求授权。
- [ ] `BLOCKED` 一次提醒设置为当前时间两分钟后，只触发一次。
- [ ] `BLOCKED` 持续提醒设置为当前时间两分钟后，最多预排 8 次；完成、关闭提醒或删除任务后取消余下请求。
- [ ] `BLOCKED` 每日统一提醒设置为当前时间两分钟后触发；关闭或修改时间会替换旧请求。

## 视觉、动效与输入

- [x] `PASS` 当前系统浅色外观下，今日页黑白层级、正文、进度、空状态和任务模块清晰；截图见 artifacts。
- [ ] `BLOCKED` 深色模式中黑白层级、正文对比度、进度与玻璃模块清晰。
- [ ] `BLOCKED` 今日空状态、快速添加和任务行可见半透明边缘与阴影，但静态截图无法可靠证明动态背景折射；菜单栏面板无法由 AX 定位，因此不记为通过。
- [ ] `BLOCKED` Computer Use 不能稳定捕获 hover/按下过程的逐帧材质与位移，无法证明不超过 2 pt 或可中断。
- [ ] `BLOCKED` 勾选、进度、材质和文字状态联动；编辑浮层沿触发位置出现/消失且输入不被动画锁定。
- [ ] `BLOCKED` 已验证四页快捷导航和快速添加聚焦；未完成纯键盘勾选、编辑、删除的端到端链路，也未可靠目视确认每个焦点轮廓。
- [x] `PASS` AX tree 暴露四个导航入口、任务标题、完成/取消完成按钮、进度、快速添加、规则、历史和设置控件；标签包含状态文字，不仅依赖颜色。未启动 VoiceOver 进程，结论限于 AX tree。
- [ ] `BLOCKED` 开启 Reduce Motion 时移除位移、惯性和弹性过冲，改为短淡化或即时重排，全部功能仍可操作。
- [ ] `BLOCKED` 开启 Reduce Transparency 时玻璃切换为高不透明度黑白表面并保留明确边框，全部功能仍可操作。
- [ ] `BLOCKED` 开启 Increase Contrast 时文字、边缘、焦点和选中状态对比度提高，全部功能仍可操作。

## 备注

- GUI 使用已签名的 `build/Daily.app` 与 `@oai/sky` 执行；未修改系统全局外观、辅助功能或隐私数据库。
- `PASS` 11 项，`FAIL` 0 项，`BLOCKED` 17 项。
- 截图：`.superpowers/sdd/2026-08-12-daily-macos-app/artifacts/today-empty.png`、`.superpowers/sdd/2026-08-12-daily-macos-app/artifacts/rules.png`。
